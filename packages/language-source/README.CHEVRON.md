# language-source (Chevron)

Shared `.source` editor settings (comment wrap + indent). No grammar.
Settings ship as JSON (`settings/language-source.json`). 13c: no CSON.

Owned so the pin is not an archived `atom/*` remote.
Chevron loads this via `packageDependencies`.
