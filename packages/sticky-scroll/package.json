{
  "name": "@builtbygio/sticky-scroll",
  "version": "0.1.0",
  "main": "./lib/main",
  "description": "Pin the lines that opened the blocks you have scrolled past.",
  "license": "MIT",
  "repository": "https://github.com/builtbygio/chevron",
  "engines": { "chevron": "*", "atom": "*" },
  "configSchema": {
    "enabled": {
      "type": "boolean",
      "default": true,
      "title": "Pin enclosing lines while scrolling"
    },
    "maxLines": {
      "type": "integer",
      "default": 5,
      "minimum": 1,
      "maximum": 10,
      "title": "Most lines to pin",
      "description": "Deep nesting would otherwise cover the code you scrolled to."
    }
  }
}
