# Recovered GraphQL (8B)

Extracted from Relay 5 `__generated__` comments. 34 documents.
These are the source of truth for rewriting queries off Relay.
Do not delete inbox views. Do not treat this as a Relay compiler input
until a later slice rewires `relay-compiler`.
